import { Link } from "react-router-dom"

function NotFound() {
    return (
        <div>
            page not found
            <Link to='search' >ser</Link>
        </div>
    )
}

export default NotFound
